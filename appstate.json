const login = require("fca-unofficial");
const fs = require("fs");

login({ email: "0858444173", password: "thangngu123" }, (err, api) => {
    if (err) return console.error(err);
    fs.writeFileSync("appstate.json", JSON.stringify(api.getAppState()));
